{
  "name": "clutch",
  "private": true,
  "scripts": {
    "start": ". proxy.conf; cd webserver; go run . --rpcUser $user --rpcPassword $password",
    "lint": "eslint components/* styles/*.js util/*"
  },
  "dependencies": {
    "lit": "3.0.2"
  },
  "devDependencies": {
    "eslint": "latest"
  }
}
